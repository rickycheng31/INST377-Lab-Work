const first_item = document.querySelector("carousel-item.visible");
const last_item = document.querySelector("carousel-item:last-of-type")
document.
    querySelector('next_button')
    .addEventListener("click", function() {
        moveToNextSlide();
    });
